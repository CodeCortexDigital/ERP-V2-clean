# Core ERP backend dependencies — install: pip install -r requirements.txt
Django>=5.2,<6.0  # tested on 5.2; Django 6 not yet verified
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
drf-spectacular>=0.27
django-filter>=24.0
python-dotenv>=1.0
factory_boy>=3.3.0
Faker>=23.0
pytest-django>=4.5
Pillow>=10.0
redis>=5.0
django-redis>=5.4
celery>=5.3

# Storage (required — listed in INSTALLED_APPS as "storages")
django-storages>=1.14
boto3>=1.34

# Real-time (Django Channels)
channels>=4.0
channels-redis>=4.2
daphne>=4.0

# Database (production)
psycopg2-binary>=2.9

# AI Assistant
openai>=1.30
anthropic>=1.8

# Firebase / Google OAuth
firebase-admin>=6.5
google-auth>=2.30
segno>=1.6  # QR codes on library labels (pure Python)
openpyxl>=3.1,<4  # reading Excel files for data import
