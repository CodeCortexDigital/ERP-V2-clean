/**
 * @deprecated Use `useAuth` from `@/hooks/useAuth` and `AuthInitializer` instead.
 * Kept for backward-compatible imports.
 */
export { useAuth } from '@/hooks/useAuth';
export { AuthInitializer as AuthProvider } from '@/providers/AuthInitializer';
